<%= form_for(game) do |g| %>
  <%= g.hidden_field :row, :value => row %>
  <%= g.hidden_field :column, :value => column %>
      
  <%= g.submit "#{row}, #{column}", class: "big-btn btn btn-large btn-primary" %>
<% end %>
